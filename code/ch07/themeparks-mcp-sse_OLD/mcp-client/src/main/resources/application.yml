spring:
  application:
    name: mcp-client
  ai:
    openai:
      chat:
        api-key: ${OPENAI_API_KEY}
        options:
          model: gpt-4o-mini

mcp.server.url: http://localhost:3001

management:
  endpoints:
    web:
      exposure:
        include: info, metrics